name: support-triage-bot
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain
      - langchain-community
      - langchain-openai
      - langgraph
      - openai
      - python-dotenv
      - faiss-cpu
      - pandas
      - PyPDF2
      - tiktoken
